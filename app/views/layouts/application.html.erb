<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Unixpo.rn</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav class="navbar navbar-toggleable-md navbar-inverse sticky-top bg-inverse">
      <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <a class="navbar-brand" href="#">UNIXpo.rn</a>
      <div class="collapse navbar-collapse" id="navbarNavDropdown">
        <ul class="navbar-nav navbar-inverse bg-inverse">
          <li class="nav-item active">
            <a class="nav-link" href="/">Front Page</a>
          </li>
          <li class="nav-item active">
            <a class="nav-link" href="/">Browse</a>
          </li>
          <li class="nav-item active">
            <a class="nav-link" href="/">Submit</a>
          </li>
        </ul>
        <ul class="navbar-nav nav navbar-right navbar-inverse bg-inverse">
          <li class="nav-item active">
            <% if user_signed_in? %>
              <%= link_to "Sign Out", destroy_user_session_path, 
              :method => :delete,
              :class => "nav-link active" %>
            <% else %>
              <%= link_to "Sign In", new_user_session_path,
              :class => "nav-link active" %>
            <% end %> 
          </li>
        </ul>
      </div>
    </nav>
    <div class="container">
      <%= yield %>
    </div>
  </body>
</html>
